<div class="container" data-controller="counter">
  <div class="flex-center mt-4">
    <div class="counter">
      <span data-counter-target="count">
        <%= @restaurant_count %>
      </span>
    </div>

    <!-- <button class="btn btn-outline-secondary"
            data-action="click->counter#refresh">
          Refresh counter
    </button> -->
  </div>
</div>
